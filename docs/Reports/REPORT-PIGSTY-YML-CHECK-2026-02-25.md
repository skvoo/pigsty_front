# Отчёт: проверка pigsty.yml перед копированием на сервер

**Дата:** 25.02.2026  
**Цель:** убедиться, что при копировании конфига на сервер не затираются отличия; при расхождениях — зафиксировать отчёт.

---

## Результат

| Параметр | Сервер (104.223.25.234) | Репозиторий (локально) | Расхождение |
|----------|-------------------------|-------------------------|-------------|
| Размер файла | 19 058 байт | 19 058 байт | **Нет** |
| MD5 | `222a1b72bb4d8851b3a770ecde9479f0` | `222A1B72BB4D8851B3A770ECDE9479F0` | **Нет** |
| Секция pgb_hba_rules | Включает правило order 103 (Imperial test from Vercel) | То же | **Нет** |

**Вывод:** версия на сервере и в репозитории совпадают. Копирование не требуется. Правило доступа к imperialdb для теста на Vercel на сервере уже присутствует.

---

## Команды для повторной проверки

**На сервере:**
```bash
wc -c ~/pigsty/pigsty.yml
md5sum ~/pigsty/pigsty.yml
sed -n '79,84p' ~/pigsty/pigsty.yml
```

**Локально (PowerShell):**
```powershell
(Get-Item .\pigsty.yml).Length
(Get-FileHash -Path .\pigsty.yml -Algorithm MD5).Hash
```

См. также: [CHECK-BEFORE-COPY-TO-SERVER.md](../CHECK-BEFORE-COPY-TO-SERVER.md).
