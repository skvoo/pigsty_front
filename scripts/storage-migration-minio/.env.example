# Supabase Cloud (source) — one project per run
OLD_PROJECT_URL=https://xxxx.supabase.co
OLD_PROJECT_SERVICE_KEY=eyJhbGc...

# MinIO (Pigsty)
MINIO_ENDPOINT=http://104.223.25.234:9000
MINIO_ACCESS_KEY=s3user_gdlounge
MINIO_SECRET_KEY=GdLoungeStorage7xKp2mNqR

# One source bucket → one target bucket per run
SOURCE_BUCKET=assets
TARGET_BUCKET=gd-lounge-assets

# Optional
# BATCH_SIZE=500
